#include<bits/stdc++.h>

using namespace std;

//Unordered set
//Lower bound and Upper bound functions don't work
//rest all the funtions are the same

//insertion of element into an unorderd set has time complexity O(1)
// in most of the cases but sometimes it may be O(n)
// The way how unordered set works is not known

// The elements are stored in a randomized way which is not in a list nor in a sorted way

int main()
{
    unordered_set<int> st;
}