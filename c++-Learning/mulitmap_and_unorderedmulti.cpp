#include<bits/stdc++.h>

using namespace std;

//multimap is the same as map but can store multiple keys
// only map[key] cannot be used

//unordered map and map has the same difference as that of set and unordered set

//insertion an element into unordered map takes O(1) and in the worst case which doesn't happen
// that easily takes O(n)
int main()
{
    
}